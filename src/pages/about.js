const AboutPage = () => {
  // const productList = useSelector((state) => state.product);
  // console.log(productList);

  return (
    <>
    <h1>Test</h1>
    </>
  );
};
export default AboutPage;
